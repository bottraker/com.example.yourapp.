<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1. Permisos OBLIGATORIOS para ubicación -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 2. Permiso OBLIGATORIO para ejecutar un servicio en primer plano (desde Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GPS Tracker"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 3. Declaración del Servicio. Sin esto, la app se cerrará con un error. -->
        <service android:name=".LocationService" android:exported="false" />

    </application>
</manifest>
